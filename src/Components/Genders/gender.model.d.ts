export interface genderCreationDTO {
  name: string;
}

export interface editGenderDTO {
  name: string;
}

export interface genderDTO {
  name: string;
}



export interface Gender {
  name: string;
  id: string;
}